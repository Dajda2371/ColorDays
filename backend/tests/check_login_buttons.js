const fs = require('fs');

const files = [
    './frontend/leaderboard.js',
    './frontend/login.js',
    './frontend/config.js',
    './frontend/change-password.js'
];

// same logic for login but it doesn't have a navbar... Wait, login has a translate toggle.
